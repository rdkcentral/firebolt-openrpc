uint32_t ${info.Title}_${Title}Array_Size(${type} handle);
${type} ${title}Array_Get(${type} handle, uint32_t index);
void ${info.Title}_${Title}Array_Add(${propertyType}Handle handle, ${valueType} value);
void ${info.Title}_${Title}Array_Clear(${propertyType}Handle handle);
