  uint32_t ${parent.title}_KeysCount(${parent.title} handle);
  void ${parent.title}_AddKey(${parent.title} handle, char* key, ${title} value);
  void ${parent.title}_RemoveKey(${parent.title} handle, char* key);
  ${title} ${parent.title}_FindKey(${parent.title} handle, char* key);
