/* ${title} */
typedef void* ${title};
${title} ${title}_Create(void);
void ${title}_Addref(${title} handle);
void ${title}_Release(${title} handle);
bool ${title}_IsValid(${title} handle);

${properties}
