    /*
     * ${method.description}
     * ${method.params}
     */
    virtual ${method.signature.result} ${method.name}( ${method.signature.params}${if.params}, ${end.if.params}Firebolt::Error *err = nullptr ) const = 0;
